//
// Created by mayak on 7/2/25.
//

#include "builtin_math/math_std.hpp"

#include <gtest/gtest.h>

// TODO: add test
// TEST(GenerateFFTSizes, HighPrecision) {
// // SET VALUES -------------------------------------------------------------------------------
// int order = 8;
// auto flag = math_std::FFTFlag::FFT_FLAG_HIGH_PRECISION;
// chord::WorkBufferSizes actual_sizes{};
//
// chord::WorkBufferSizes expected_sizes = {6016, 3008, 2048};
//
// // CALL FUNCTION ----------------------------------------------------------------------------
// math_std::generate_FFT_sizes(order, flag, actual_sizes);
//
// // COMAPRE RESULTS --------------------------------------------------------------------------
// ASSERT_EQ(expected_sizes.size_spec, actual_sizes.size_spec);
// ASSERT_EQ(expected_sizes.size_buffer, actual_sizes.size_buffer);
// ASSERT_EQ(expected_sizes.size_init, actual_sizes.size_init);
// }
